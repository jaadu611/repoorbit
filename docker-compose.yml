services:
  repoorbit:
    build: .
    ports:
      - "3000:3000"
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./scratch:/app/scratch
      - ./notebooklm_sources:/app/notebooklm_sources
    restart: unless-stopped
